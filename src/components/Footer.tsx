import AppBar from '@mui/material/AppBar';
import Typography from '@mui/material/Typography';

function Footer() {
    return (
        <header style={{ marginTop: "100px" }}>
            <AppBar position="static" style={{ height: "50px", background: '#2E3B55' }}>
            </AppBar>
        </header>
    );
}

export default Footer;